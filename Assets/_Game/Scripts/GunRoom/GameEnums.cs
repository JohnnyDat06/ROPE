public enum CheckMode
{
    Quantity, // Số lượng
    Weight    // Cân nặng (Kg)
}

public enum ConditionType
{
    Lower,   // Thấp hơn
    Equal,   // Bằng (sai số cực thấp)
    Higher   // Cao hơn
}